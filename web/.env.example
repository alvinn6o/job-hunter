# Postgres (Neon or any PostgreSQL)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Resume parsing (Gemini AI)
GEMINI_API_KEY="your-gemini-api-key"

# Protect /api/cron endpoints (must match GitHub Actions secret)
CRON_SECRET="your-cron-secret"

# Optional: Worker endpoint (legacy — not needed for GitHub Actions mode)
# WORKER_URL="https://your-worker.railway.app"
# WORKER_SECRET="your-shared-secret"
